(function() {
    'use strict';
    angular
        .module('app', []);

    /*
    var item = someDynamicValue;
    switch(item) {
      case 'a':
      // do something when the `item` is a
      case 'b':
      // do something when the `item` is b
      case 'c':
      // do something when the `item` is c
      default:
      // do something when the `item` is DEFAULT
    }
    */
})();
